# Scope

MVP: iOS and Android text chats, groups, E2EE integration, local queue, relay failover, WebSocket plus HTTPS fallback, push, RU/EN.

Excluded: LoRa, mesh, taxi/vehicle relays, rescue/satellite/hardware, calls, video, voice notes, media/files, public channels, stories, bots, payments, web client, federation.
