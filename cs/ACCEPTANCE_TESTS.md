# Acceptance tests

CI must format, lint, test and build the Rust workspace without production secrets. Docker Compose must parse. Later stages add 1 Kbit/s network simulations, reconnect delivery and protocol test vectors.
